yarr soul is an empty pile of stuff!

yar!

label: <input name="key" value="<%= model.escape('key') %>" autofocus />

type: <input name="value[type]" value="<%= _.escape(model.get('value').type) %>" />
default value: <input name="value[value]" value="<%= _.escape(model.get('value').value) %>" />
